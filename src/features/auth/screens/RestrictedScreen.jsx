import { View, Text } from "react-native"
export default function RestrictedScreen(){
  return (
    <View style={{flex:1,justifyContent:"center",alignItems:"center",padding:24}}>
      <Text>Restricted (guest-only placeholder)</Text>
    </View>
  )
}
