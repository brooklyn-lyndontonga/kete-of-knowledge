services:
  # 1. The Backend API (Node.js + SQLite)
  - type: web
    name: kete-server
    env: docker
    dockerContext: .
    dockerfilePath: clean/server/Dockerfile
    plan: free
    disk:
      name: sqlite-data
      mountPath: /app/db
      sizeGB: 1
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
      - key: SQLITE_DB_PATH
        value: /app/db/database.db
      - key: JWT_SECRET
        generateValue: true
      # EMAIL SERVICE (Required for Magic Links)
      # Replace these with your actual SMTP credentials (e.g. Gmail App Password, Brevo, SendGrid)
      - key: SMTP_HOST
        value: smtp.example.com
      - key: SMTP_PORT
        value: 587
      - key: SMTP_USER
        value: your_email@example.com
      - key: SMTP_PASS
        value: your_password

  # 2. The Admin Panel (React/Vite)
  - type: static
    name: kete-admin
    env: static
    buildCommand: cd clean/admin && npm install && npm run build
    staticPublishPath: clean/admin/dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: kete-server
          property: url

version: "1"
