# Admin JWT auth
ADMIN_JWT_SECRET=change_me_super_secret
ADMIN_SEED_EMAIL=admin@example.com
ADMIN_SEED_PASSWORD=change_me_now

# Email Service (Nodemailer)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=user
SMTP_PASS=pass
SMTP_SECURE=false
SMTP_FROM="Kete of Knowledge" <no-reply@kete.com>

# App JWT Secret (if separate) or use ADMIN_JWT_SECRET if shared
JWT_SECRET=change_me_app_secret
