version: '3.8'

services:
  server:
    build: ./server
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    env_file:
      - ./server/.env
    volumes:
      - sqlite_data:/app/db

volumes:
  sqlite_data:
